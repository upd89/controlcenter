<%= form_for(@task_execution) do |f| %>
  <% if @task_execution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_execution.errors.count, "error") %> prohibited this task_execution from being saved:</h2>

      <ul>
      <% @task_execution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <ul class="properties col_6">
    <li>
      <p>
        <span class="property"><%= f.label :log %></span>
        <span class="value"><%= f.text_field :log %></span>
      </p>
    </li>
  </ul>

  <div class="actions">
    <%= f.button "Save", :class => "button" %>
  </div>

<% end %>

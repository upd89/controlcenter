<%= form_for(@package_installation) do |f| %>
  <% if @package_installation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package_installation.errors.count, "error") %> prohibited this package_installation from being saved:</h2>

      <ul>
      <% @package_installation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <ul class="properties col_6">
    <li>
      <p>
        <span class="property"><%= f.label :installed_version %></span>
        <span class="value"><%= f.text_field :installed_version %></span>
      </p>
    </li>
    <li>
      <p>
        <span class="property"><%= f.label :package_id %></span>
        <span class="value"><%= f.collection_select(:package_id, Package.all, :id, :name) %></span>
      </p>
    </li>
    <li>
      <p>
        <span class="property"><%= f.label :system_id %></span>
        <span class="value"><%= f.collection_select(:system_id, System.all, :id, :name) %></span>
      </p>
    </li>
  </ul>

  <div class="actions">
    <%= f.button "Save", :class => "button" %>
  </div>
<% end %>

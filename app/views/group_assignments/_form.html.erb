<%= form_for(@group_assignment) do |f| %>
  <% if @group_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_assignment.errors.count, "error") %> prohibited this group_assignment from being saved:</h2>

      <ul>
      <% @group_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <ul class="properties col_6">
    <li>
      <p>
        <span class="property"><%= f.label :package_group_id %></span>
        <span class="value"><%= f.collection_select(:package_group_id, PackageGroup.all, :id, :name) %></span>
      </p>
    </li>
    <li>
      <p>
        <span class="property"><%= f.label :package_id %></span>
        <span class="value"><%= f.collection_select(:package_id, Package.all, :id, :name) %></span>
      </p>
    </li>
  </ul>

  <div class="actions">
    <%= f.button "Save", :class => "button" %>
  </div>
<% end %>

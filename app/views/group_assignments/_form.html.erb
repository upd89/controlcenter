<%= form_for(@group_assignment) do |f| %>
  <% if @group_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_assignment.errors.count, "error") %> prohibited this group_assignment from being saved:</h2>

      <ul>
      <% @group_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :package_group_id %><br>
    <%= f.collection_select(:package_group_id, PackageGroup.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :package_id %><br>
    <%= f.collection_select(:package_id, Package.all, :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

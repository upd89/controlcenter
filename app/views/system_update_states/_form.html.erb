<%= form_for(@system_update_state) do |f| %>
  <% if @system_update_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@system_update_state.errors.count, "error") %> prohibited this system_update_state from being saved:</h2>

      <ul>
      <% @system_update_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <ul class="properties col_6">
    <li>
      <p>
        <span class="property"><%= f.label :name %></span>
        <span class="value"><%= f.text_field :name %></span>
      </p>
    </li>
  </ul>

  <div class="actions">
    <%= f.button "Save", :class => "button" %>
  </div>
<% end %>

<%= form_for(@system_group) do |f| %>
  <% if @system_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@system_group.errors.count, "error") %> prohibited this system_group from being saved:</h2>

      <ul>
      <% @system_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :permission_level %><br>
    <%= f.number_field :permission_level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

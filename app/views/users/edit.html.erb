<%= render :partial => "header", :object => {
  "title" => "Edit " + @user.name,
  "actions" =>[{
    "icon" => "arrow-left",
    "text" => "",
    "path" => users_path
  }, {
    "icon" => "eye",
    "text" => "",
    "path" => @user
  }, {
    "icon" => "remove",
    "text" => "",
    "path" => @user,
    "method" => :delete,
    "confirm" => "Are you sure you want to delete this user?"
  }]
} %>

<%= render 'form' %>

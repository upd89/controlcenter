<%= form_for(@concrete_package_state) do |f| %>
  <% if @concrete_package_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concrete_package_state.errors.count, "error") %> prohibited this concrete_package_state from being saved:</h2>

      <ul>
      <% @concrete_package_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <ul class="properties col_6">
      <li>
      <p>
          <span class="property">name</span>
        <span class="value"><%= f.text_field :name %></span>
            </p>
    </li>
    </ul>

  <div class="actions">
    <%= f.button "Save", :class => "button" %>
  </div>

<% end %>

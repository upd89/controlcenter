<%= render :partial => "header", :object => {
  "title" => "New Distribution",
  "actions" =>[{
    "icon" => "arrow-left",
    "text" => "",
    "path" => distributions_path
  }]
} %>

<%= render 'form' %>

<div class="floatFix"></div>


<%= form_for(@package_update) do |f| %>
  <% if @package_update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package_update.errors.count, "error") %> prohibited this package_update from being saved:</h2>

      <ul>
      <% @package_update.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <ul class="properties col_6">
    <li>
      <p>
        <span class="property"><%= f.label :candidate_version %></span>
        <span class="value"><%= f.text_field :candidate_version %></span>
      </p>
    </li>
    <li>
      <p>
        <span class="property"><%= f.label :repository %></span>
        <span class="value"><%= f.text_field :repository %></span>
      </p>
    </li>
    <li>
      <p>
        <span class="property"><%= f.label :package_id %></span>
        <span class="value"><%= f.collection_select(:package_id, Package.all, :id, :name) %></span>
      </p>
    </li>
  </ul>

  <div class="actions">
    <%= f.button "Save", :class => "button" %>
  </div>
<% end %>

<%= form_for(@package_group) do |f| %>
  <% if @package_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@package_group.errors.count, "error") %> prohibited this package_group from being saved:</h2>

      <ul>
      <% @package_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <ul class="properties col_6">
    <li>
      <p>
        <span class="property"><%= f.label :name %></span>
        <span class="value"><%= f.text_field :name %></span>
      </p>
    </li>
    <li>
      <p>
        <span class="property"><%= f.label :permission_level %></span>
        <span class="value"><%= f.number_field :permission_level %></span>
      </p>
    </li>
  </ul>

  <div class="actions">
    <%= f.button "Save", :class => "button" %>
  </div>
<% end %>

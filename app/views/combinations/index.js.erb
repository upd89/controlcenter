<% js = escape_javascript(
  render(partial: 'combinations/list', locals: {
    packages: @packages,
    systems: @systems
    })
) %>
$("#filterrific_table").html("<%= js %>");

// make sure rows are clickable!
$(document).trigger("page:change")
